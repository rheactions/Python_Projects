#Write a Python function that takes a list of
# numbers and returns the largest and smallest
# elements.
from operator import index

numbers = [2,5,8,3]

largest = max(numbers)
print(largest)

smallest = min(numbers)
print(smallest)